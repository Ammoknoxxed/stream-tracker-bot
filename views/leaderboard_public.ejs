<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Leaderboard - <%= guild.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #1a1a2e; color: #fff; font-family: 'Segoe UI', sans-serif; }
        .leaderboard-container { max-width: 800px; margin: 50px auto; }
        .card { background-color: #16213e; border: none; border-radius: 20px; overflow: hidden; }
        .header-box { background: linear-gradient(45deg, #0f3460, #e94560); padding: 30px; text-align: center; }
        .table { color: #fff; margin-bottom: 0; }
        .rank-1 { color: #ffd700; }
        .rank-2 { color: #c0c0c0; }
        .rank-3 { color: #cd7f32; }
        .avatar { width: 40px; height: 40px; border-radius: 50%; margin-right: 10px; }
        .live-dot { height: 10px; width: 10px; background-color: #ff4b2b; border-radius: 50%; display: inline-block; margin-right: 5px; animation: pulse 1.5s infinite; }
        @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.3; } 100% { opacity: 1; } }
    </style>
</head>
<body>
<div class="container leaderboard-container">
    <div class="card shadow-lg">
        <div class="header-box">
            <h1 class="mb-0"><i class="fas fa-crown me-2"></i> Stream-Ranking</h1>
            <p class="mb-0 opacity-75"><%= guild.name %></p>
        </div>
        <div class="card-body p-0">
            <table class="table table-hover align-middle">
                <thead class="bg-dark text-uppercase small opacity-50">
                    <tr>
                        <th class="ps-4">#</th>
                        <th>User</th>
                        <th class="text-end pe-4">Zeit</th>
                    </tr>
                </thead>
                <tbody>
                    <% trackedUsers.forEach((user, index) => { %>
                    <tr>
                        <td class="ps-4">
                            <% if(index === 0) { %><i class="fas fa-trophy rank-1"></i>
                            <% } else if(index === 1) { %><i class="fas fa-medal rank-2"></i>
                            <% } else if(index === 2) { %><i class="fas fa-medal rank-3"></i>
                            <% } else { %> <%= index + 1 %> <% } %>
                        </td>
                        <td>
                            <div class="d-flex align-items-center">
                                <% if(user.isStreaming) { %><span class="live-dot"></span><% } %>
                                <span><%= user.username %></span>
                            </div>
                        </td>
                        <td class="text-end pe-4 fw-bold text-info">
                            <%= user.totalMinutes %> <span class="small opacity-50 fw-normal">Min.</span>
                        </td>
                    </tr>
                    <% }) %>
                    <% if(trackedUsers.length === 0) { %>
                        <tr><td colspan="3" class="text-center py-5">Noch keine Stream-Daten vorhanden.</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
    <div class="text-center mt-4 opacity-50 small">
        Aktualisiert sich automatisch bei Stream-Ende
    </div>
</div>
</body>
</html>
