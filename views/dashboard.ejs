<!DOCTYPE html>
<html lang="de">
<head>
    <link rel="stylesheet" href="/style.css">
    <meta charset="UTF-8">
    <title>Deine Server</title>
    <style>
        body { font-family: sans-serif; background: #23272a; color: white; padding: 20px; }
        .guild-card { background: #2c2f33; padding: 15px; margin: 10px; border-radius: 8px; display: inline-block; width: 250px; }
        .btn { background: #43b581; color: white; padding: 8px 12px; text-decoration: none; border-radius: 4px; }
    </style>
</head>
<body>
    <h1>Hallo, <%= user.username %>!</h1>
    <p>Wähle einen Server aus, um ihn zu konfigurieren:</p>
    <div id="guilds">
        <% guilds.forEach(guild => { %>
            <div class="guild-card">
                <h3><%= guild.name %></h3>
                <a href="/dashboard/<%= guild.id %>" class="btn">Einstellungen</a>
            </div>
        <% }) %>
    </div>
    <br>
    <a href="/logout" style="color: #f04747;">Logout</a>
</body>

</html>
